import * as assert from 'assert';

import { TestHelper } from '../../../helpers/testHelper';

suite('Проверка форматирования модульных тестов корреляций', () => {
  // 	test('Проверка форматирования', async() => {
  // 		const inputData = `# Comment input 1\n\n#Comment input2\n\n\n# Тут будет твой тест.
  // {"msgid":"4624","normalized":true,"object":"system","object.property":"session","object.value":"0","recv_ipv4":"127.0.0.1"}\n\n
  // `;
  // 		const expected = `# Comment input 1\n# Comment input2\n# Тут будет твой тест.
  // {"msgid":"4624","normalized":true,"object":"system","object.property":"session","object.value":"0","recv_ipv4":"127.0.0.1"}
  // `;
  // 		const actual = TestHelper.formatUnitTest(inputData);
  // 		assert.strictEqual(actual, expected);
  // 	});
});
