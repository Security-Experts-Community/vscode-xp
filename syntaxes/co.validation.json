[
	{
		"desc": "Ошибка присвоения значения. Проверьте наличие знака '$'.",
		"regex": "^\\s*([A-Za-z\\.0-9_]+\\s*=\\s*[A-Za-z\\.0-9_]+\\S+)$",
		"_comment": "В таких случаях фолзит, поправить можно таким образом."
	},
	{
		"desc": "Ошибка присвоения значения. Проверьте наличие знака '$'.",
		"regex": "^\\s*\\$incident\\.severity\\s*=\\s*\\$importance\\s*$",
		"_comment": "В таких случаях фолзит, поправить можно таким образом."
	},
	{
		"desc": "Сравнение значения в нижнем регистре с литералом с верхнем регистре",
		"regex": "lower([^)]+)\\s*==\\s*\"[A-Z0-9]+\"",
		"_comment": "В таких случаях фолзит, поправить можно таким образом."
	},
	{
		"desc": "Некорректный проброс importance в incident.severity",
		"regex": "\\$importance = \"info\".*?\\$incident\\.severity = \\$importance",
		"_comment": "У поля incident.severity нет значения info, поэтому такой проброс полей нельзя использовать"
	}
]